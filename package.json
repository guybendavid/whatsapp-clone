{
  "name": "whatsapp-clone",
  "version": "1.0.0",
  "author": "Guy Ben David",
  "license": "BSD-3-Clause",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev:client": "npm -C client run dev",
    "dev:server": "npm -C server run dev",
    "dev": "concurrently -k -n client,server -c cyan,magenta \"npm run dev:client\" \"npm run dev:server\"",
    "start:client": "cd client && npx vite preview",
    "start:server": "node dist/app.js",
    "start": "concurrently -k -n client,server -c cyan,magenta \"npm run start:client\" \"npm run start:server\"",
    "format": "prettier --write .",
    "lint": "eslint . && npx strictify unimported-exports && knip --no-exit-code && npm run --silent knip:validate-paths",
    "lint:eslint-fix": "eslint . --fix",
    "knip:validate-paths": "npx strictify validate-knip-paths",
    "test": "node --test --import tsx tests/**/*.test.*",
    "build-client": "cd client && npx tsc -b tsconfig.json && npx vite build",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
    "create-dist-folder": "node -e \"require('fs').mkdirSync('dist', { recursive: true })\"",
    "build-server": "cd server && npm run build && cd .. && npx -y cpy-cli server/package.json dist --flat && node -e \"const fs=require('fs');const path=require('path');const src=path.join('server','package-lock.json');const dest=path.join('dist','package-lock.json');if(fs.existsSync(src)) fs.copyFileSync(src,dest);\" && (cd dist && npm i --no-audit --no-fund)",
    "build": "npm run clean && npm run create-dist-folder && npm run build-client && npx -y cpy-cli \"client/build/**\" dist/client --cwd=. --parents && node -e \"require('fs').rmSync('client/build', { recursive: true, force: true })\" && npm run build-server",
    "prepare": "husky",
    "validate": "npm run lint && npm run test && npx prettier --check ."
  },
  "devDependencies": {
    "@types/eslint-plugin-jsx-a11y": "^6.10.1",
    "concurrently": "^9.1.2",
    "eslint": "^9.17.0",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "eslint-plugin-react": "^7.37.3",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-react-refresh": "^0.4.16",
    "husky": "^9.1.7",
    "knip": "^5.61.3",
    "prettier": "^3.6.2",
    "pretty-quick": "^4.0.0",
    "strictify": "file:../strictify",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.19.1"
  },
  "imports": {
    "#root/*": "./*"
  },
  "prettier": "strictify/prettier"
}
